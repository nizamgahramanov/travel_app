import 'package:flutter/material.dart';

class StaggeredGridItem extends StatelessWidget {
  final String name;
  final String region;
  final String photo;

  const StaggeredGridItem(
      {required this.name, required this.region, required this.photo, Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
